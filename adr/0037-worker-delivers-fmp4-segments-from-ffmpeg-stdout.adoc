= ADR 0037: The Worker Delivers Fragmented MP4 Segments From FFmpeg's Standard Output, and HLS Media Segments Are fMP4 Only

== Status

Accepted

This decision supersedes the container clause of link:0009-h264-plus-av1-codec-strategy.adoc[ADR 0009] ("H.264 uses MPEGTS container"; "container format is determined by codec choice").
This decision changes one consequence of link:0008-server-generated-hls-playlists.adoc[ADR 0008]: FFmpeg no longer writes segments to disk.
This decision leaves ADR 0009's codec-preference clause alone; the playback capabilities epic (streamarr-server issue #399) amends that clause when verified encoders and HEVC preference land.

== Context

The transcode worker runs FFmpeg under `ProcessBuilder` (link:0004-processbuilder-over-jni.adoc[ADR 0004]).
FFmpeg's HLS muxer writes each segment to a directory, and the worker uploads each segment to the server over the gRPC session that link:0018-live-playback-authority-and-outbound-workers.adoc[ADR 0018] and link:0033-transcode-worker-is-a-separate-service.adoc[ADR 0033] describe.
FFmpeg never tells the worker that a segment is complete.
The worker instead infers that a segment is complete from two independent side channels: the segment file appears in the directory when FFmpeg renames its temporary file, and the FFmpeg process exits.
The worker polls both channels every 50 ms and reads them at different moments, so each order in which the two facts can arrive produces a separate bug.
Worker issue #36 is one such bug: FFmpeg renamed the last segment and exited between the worker's two reads, so the worker reported a successful attempt as `TRANSCODE_FAILED` while the segment sat on disk.
Reviewing the fix in worker PR #38 surfaced a second bug: when the worker stops FFmpeg, FFmpeg flushes a final segment, the worker uploads that segment after it has sent `JobAttemptStopped`, the server rejects the upload, and the worker reports a failure for an attempt it has already reported as stopped.
Five open-source media and live-streaming servers that watch FFmpeg's output directory hit the same last-segment race.
One now rescans the output directory after FFmpeg exits (2026), one replaced its polling watcher in 2020 so that FFmpeg pushes each segment to the server over HTTP, and one never fixed the race.

The research behind this decision compared five designs: a producer module that watches the output directory with inotify and treats exit as an event; a loopback HTTP receiver inside the worker that accepts the segments FFmpeg's HLS muxer `PUT`s; FFmpeg `PUT`ing segments straight to the server; the server reading segments from storage the worker shares; and the worker reading fragmented MP4 from FFmpeg's standard output.
Two independent throwaway proofs of concept measured the HTTP receiver and the standard-output design against FFmpeg 8.1.2 in the pinned worker image; the second one added 17 timing cases, 13 gRPC scenarios and a playback matrix on real decoders.
FFmpeg's HTTP output never reads the response status (`libavformat/http.c` substitutes a 200), never re-sends a segment the receiver dropped, exits with status 0 even after a rejected upload, and exits before a slow receiver has read everything FFmpeg wrote.
A receiver therefore has to rebuild every delivery guarantee on its own.
FFmpeg's standard output is one ordered channel: FFmpeg writes every fragment in order, and the pipe reaches end of file only after FFmpeg has written its last byte.
The kernel pipe blocks FFmpeg when the reader falls behind, and `Process.waitFor` returns the exit code.

The mp4 muxer decides where a fragment ends, and the two muxers measure their cut points differently.
The HLS muxer cuts at the first keyframe whose presentation time, measured from the first packet of the run, reaches the segment period times the segment count (`hlsenc.c` compares `pkt->pts - vs->start_pts` against `recording_time * number`).
In the pinned FFmpeg that first packet can be audio: `hlsenc.c` keeps the first audio packet's `pts` in audio ticks unless a video packet's raw `pts` is smaller, and subtracts it in the video time base, so its reference depends on the audio sample rate; FFmpeg's development branch (checked at commit `a6e5d5f`) already rescales both to microseconds, so the HLS muxer's cut points would move with an FFmpeg update.
The mp4 muxer with `frag_keyframe` cuts at a keyframe when the time since the previous fragment reaches `min_frag_duration`, measured from the previous fragment rather than from any target.
With a constant frame rate and a forced keyframe at every period, the two rules agree, and the first proof of concept confirmed that at 23.976 fps for encode, stream copy and a seek.
They disagree whenever the keyframe spacing is not the period: on a 66 s stream copy whose keyframes sit at 0, 6.5, 12, 18, 24.5, 30, 36.5, 42, 48, 54.5 and 60 s, `min_frag_duration` just under 6 s produced 7 fragments where the HLS muxer produced 11 segments, because the 5.5 s gap from 6.5 s to 12 s can never satisfy a 5.958 s minimum.
On a variable-frame-rate encode with the worker's own forced keyframes, the same recipe produced 9 fragments where the HLS muxer produced 11 segments.
With `min_frag_duration` set to exactly the period, FFmpeg merged two segments even at a constant 23.976 fps, because the forced-keyframe gap fell one frame short.
Setting no minimum, so that every keyframe starts a fragment, and then grouping fragments by absolute presentation time reproduced the HLS muxer's cut points on all three fixtures (90 fragments into 30 segments for the NTSC copy, 11 into 11 for the irregular copy, 11 into 11 for the variable-frame-rate encode), and the pinned FFmpeg decoded every grouped playlist.
The recorded fixtures of worker issue #39, which force keyframes with the list that the Decision below adopts, qualify that agreement: grouping matches the HLS muxer tick for tick on a constant-rate encode and a stream copy whose first frame is at zero, and the recorder lists every other difference with its cause.
The variable-frame-rate encode still yields 11 segments for 11, but its first frame is at 41.7 ms and the HLS muxer measures from it: the list forces the first frame at or after each boundary, which comes before that boundary plus 41.7 ms, so the HLS muxer cuts only at the next forced keyframe, and from segment 1 on its segment numbers run one behind the grid's.
An encoded replacement attempt from segment 5 forces the same frames as the start-0 attempt, so its segments match the start-0 attempt's HLS output, while the HLS muxer's own run of its seek to 24 s measures from its first frame at 24.024 s and runs one segment behind from segment 7 on.
Fragment decode times do not land on the nominal grid: at 23.976 fps a 144-frame period is 6.006 s, the `cmaf` flag writes negative composition offsets that shift `tfdt` by two frames, and the first audio `tfdt` is the unsigned wrap of −1024 ticks; a check of `tfdt` against segment number times period rejected 23 of 30 valid segment starts.
Nor can a worker compute a fragment's end from the fragment's own sample durations: on a variable-frame-rate stream copy with B-frames, a fragment that starts at 6.0 s carries durations that sum to 12.0 s, yet the next fragment starts at 11.9 s, so a worker that closed the segment on the computed end would close it before its last fragment arrived.
The only reliable boundary signal is the next fragment's own start, and FFmpeg emits a fragment only when it closes it, so the worker learns that a segment is complete one fragment late.
Adding `-frag_duration` bounds that lag: with a 1 s bound and `frag_keyframe`, the mp4 muxer still cut at every keyframe and also cut every 1.001 s (91 fragments over 90 s at 23.976 fps, 90 for a stream copy with 2.002 s keyframes), and every fragment that did not begin at a keyframe began with a non-sync sample.
A fragment without a video track appears only after the last video sample, none, one or several (one per target of audio that outlasts the video; three AAC samples at 89.941 s on the 90 s fixture, five fragments when audio outlasts video by 5 s), and a video-only tail appears when video outlasts audio; `delay_moov` folds audio that starts before the video into the first fragment, so no fragment without video ever precedes the first segment.
On a 10 s-GOP stream copy the sync-first fragments fell in segment numbers 0, 1, 3, 5, 6, 8, 10, 11 and 13, with the first skip at 20.02 s.
moq-mux takes the same shape to its extreme: its demo runs FFmpeg with `separate_moof` and a 1 ms `-frag_duration`, so fragments are sample-sized, and its importer closes a group only when it observes the next boundary.
Keyframes do not define that importer's groups on their own: it rolls a group on every track, audio included, at each segment boundary, and a `styp` box or an explicit cut marks a boundary as well as a video keyframe does.

`-copyts` keeps the source's own timestamp origin: a source whose timestamps begin at 12 s put its first fragment at 12 s, which is segment 2, for a job that asked for segment 0.
Today's HLS muxer hides that origin by measuring from the run's first packet.
FFmpeg's `-start_at_zero` subtracts the source container's start time under `-copyts`, and that start time does not move with `-ss`: with it, the same source's first fragment started at 0 for stream copy and for encode, an encoded seek to 30 s started at 30.030 s, and a stream-copy seek to 30 s started at 28.028 s.
The container's start time is the earliest stream's first timestamp: on MPEG-TS sources it is the AAC priming frame, 21 ms before the first video frame, so video starts at 0.021 s (copy) or 0.042 s (encode) rather than at 0, and two MPEG-TS remuxes of the same content starting at 1.46 s and at 3601.38 s produced byte-identical outputs.
MPEG-TS input seeking differs from MP4 seeking: FFmpeg lands on the next keyframe after the target, not the one at or before it, so `-ss 30` on an MPEG-TS source started video at 30.051 s (copy) and 30.072 s (encode) while audio still started near 30 s, and on a 10 s-GOP MPEG-TS source `-ss 25` started video at 30.07 s with five seconds of audio before it.
`delay_moov` stops the mp4 muxer from inserting the `aac_adtstoasc` bitstream filter on its own, so a stream copy of AAC from an MPEG-TS source exits 255 with "Malformed AAC bitstream detected"; adding `-bsf:a aac_adtstoasc` fixes that copy and leaves copies from MP4 and MKV sources byte-identical.
`-max_delay 5000000`, which the worker passes today, changes nothing in mp4 output (byte-identical with and without it) while it moves the first MPEG-TS timestamp from 1.4 s to 10.0 s.
A stream-copy seek lands on the keyframe at or before the requested time, so that 28.028 s fragment is preroll.
The initialization segment does not depend on the start point: across start 0, a seek to 30 s and a seek to 60 s, every encode run produced one byte-identical `ftyp`+`moov` (1327 bytes) and every stream-copy run another (1347 bytes), with zero durations, empty sample tables, a zero edit list and zero `trex` defaults in each; pairing the start-0 init with a seek run's segments gave the same packets, and the same decoded frames, as that run's own init.
Encode and stream-copy inits differ in bitrate metadata, and a copy from an MKV source takes the source's 1/1000 time base, so an init is a property of the variant's mode and codec parameters, never of the attempt.
A smaller fact holds across attempts as well: FFmpeg's demuxer presents every output 2002 ticks (83 ms) after its composition time, the negative of the most negative composition offset the `cmaf` flag writes, uniformly and independent of the init.
Today's HLS muxer measures its cut points from that preroll keyframe, so with 48 kHz audio the segments of a stream-copy replacement attempt start at 28.028, 34.034 and 40.040 s while the server advertises them as 30, 36 and 42 s.
With 8 kHz audio the same replacement attempt took the first audio packet as its reference, read in video ticks, and cut at every 2.002 s keyframe from 28.028 s to 36.036 s and then at 40.040 s and every 6 s after: 10 segments numbered 5 to 14 where 48 kHz audio gave 7.
With every keyframe as its own fragment, the preroll is a separate fragment that the worker can discard; an encoded replacement attempt that seeks exactly to its first boundary starts at 30.030 s with nothing to discard, and the Decision below has it seek one period earlier and discard that period instead.
FFmpeg's automatic frame synchronization depends on the muxer: the mp4 muxer declares `AVFMT_VARIABLE_FPS`, so automatic mode passes variable frame rates through, while the HLS muxer does not, so automatic mode picks constant rate, and under `-copyts` FFmpeg turns that constant rate into an internal variant that keeps the first timestamp (`fftools/ffmpeg_mux_init.c`, `VSYNC_VSCFR`); the same variable-frame-rate source yielded 622 video packets through mp4 and 1976 through HLS.
The command line cannot request that internal variant, and the explicit `cfr` mode is a different thing: after a seek to 30 s it padded the output with 720 duplicate frames from time zero (2157 video packets instead of 1437), while the HLS recipe with automatic mode produced 1437 packets starting at 30.030 s.
Passing `-r` with the probed frame rate and no `-fps_mode` puts automatic mode back on the constant-rate path, and `-copyts` then selects that same internal variant: on an irregular variable-frame-rate source, SVT-AV1 with the worker's frame-count GOP produced a keyframe every 6.006 s and 1583 video packets, identical to the HLS muxer's output on the same source, and a seek to 30 s started at 30.113 s with no padding.
The same encoder under `-fps_mode vfr` placed keyframes at 0.04, 9.43, 20.35, 30.86, 40.96, 51.30 and 61.23 s, leaving five of eleven segment numbers without a keyframe, because a GOP counted in frames only equals the period at a constant frame rate; the worker gives SVT-AV1 and the hardware encoders a frame-count GOP and no time-based forced keyframe.
libx264 with time-based forced keyframes kept a keyframe in every interval under `vfr`.
A constant rate does not make a frame-count GOP fit the grid either: the worker rounds the GOP up to 144 frames at 23.976 fps, which is 6.006 s, so the keyframes drift 6 ms per segment, and after 1000 segments the keyframes at 5999.994 s and 6006.000 s leave segment 1000 with none; a feature-length transcode through a frame-count encoder would fail at 100 minutes, where today's HLS muxer silently drifts the content instead.
SVT-AV1 in the pinned build honours the time-based forced keyframe: with a 480-frame GOP and no forcing it cut at 0, 20.02, 40.04, 60.06 and 80.08 s, and with `-force_key_frames` it cut at 6.006 s intervals on exactly the frames libx264 chose, including the one-frame-early 42.000 s, which that recording forced with an expression measured from the run's first frame, so it is the first frame at or after 42 s only when that frame is at zero (on the recorded variable-frame-rate source, whose first frame is at 41.7 ms, the expression put that keyframe at 42.042 s for both encoders, and the list puts it at 42.0003 s); the worker today gives SVT-AV1 no forced keyframe because of an unrelated `-forced-idr` warning.
A GOP rounded down to 143 frames (5.964 s), the floored GOP, placed a keyframe inside every one of the fifteen intervals on its own, with the phase wrapping once every 167 segments.
Recordings under the pinned FFmpeg, made while the recipe below still forced keyframes with the expression `gte(t,n_forced*period)` instead of its list, measured what that floor costs once forced keyframes are on, for libx264, libx265 (with `scenecut=0`, which the worker does not pass) and SVT-AV1 at 23.976 and 29.97 fps, each from the start and after a seek to 30 s.
All three encoders count their GOP from the most recent keyframe, forced or not, so wherever two forced keyframes lie one frame farther apart than the floored GOP (144 frames against 143 at 23.976 fps, 180 against 179 at 29.97 fps), the floored GOP adds a second keyframe one frame before the forced one.
Over 96 s from the start that happened at 13 of 15 boundaries at both rates, which is 857 and 821 of every 1000 boundaries by arithmetic, and each run's last frame became a keyframe too; after the seek it happened at 9 of 10 boundaries, and there the extra keyframe mostly fell on the interval's first frame, one frame before the forced keyframe, which the expression measured from the seek's first frame.
Each extra keyframe costs a second intra-coded frame and a one-frame fragment inside a segment that already has its keyframe.
With the extended GOP, the rounded-up number of frames plus one (145 at 23.976 fps, 181 at 29.97 fps), the same twelve runs held no keyframe except the forced ones and exactly one keyframe-first fragment in every interval.
With one forced keyframe suppressed, the extended GOP placed a keyframe one frame after the missing one (24.066 s and 42.084 s at 23.976 fps, 24.057 s and 42.075 s at 29.97 fps), inside the interval that the forced keyframe would have opened.
The three encoders placed every keyframe on the same frames in every run.
libx264 wrote every keyframe as an IDR picture and SVT-AV1 every keyframe as a shown AV1 key frame, which releases every reference frame, so no later frame of either encoder references a frame before a keyframe.
libx265, under its default open GOP, wrote every forced keyframe after the first as a CRA picture (HEVC NAL unit type 21), not an IDR picture, despite `-forced-idr 1`, and the mp4 muxer marked each one a sync sample; only its first keyframe was an IDR picture.
A CRA picture may be followed in decode order by RASL pictures, which precede it in presentation order and reference the GOP before it; a decoder that starts at the CRA picture must discard them, so a segment that starts there does not decode on its own.
Under the extended GOP no RASL picture followed a forced CRA picture, but with one forced keyframe suppressed, the keyframe that libx265's GOP placed instead was a CRA picture followed by one or two RASL pictures in three of the four recordings, and under the floored GOP each recording held 3 to 23 RASL pictures.

A separate study, which worker issue #42 records, ran SVT-AV1 1,170 times through the recipe with the expression in place of the list, in the pinned runtime and in Homebrew FFmpeg 8.0.1 with SVT-AV1 4.0.1 and 4.1.0, at periods of 1 s and 6 s under both GOPs: in every run whose packets left the encoder in order, every forced keyframe was a shown AV1 key frame, a sync sample and a fragment start, and under the extended GOP no other keyframe appeared.
The same runs found that the pinned runtime, FFmpeg 8.1.2 with SVT-AV1 4.1.0 (ADR 0029), carries upstream SVT-AV1 bug #2385: when the encoder's oldest picture is slow, a later picture overwrites its slot in the packetization reorder queue, and the encoder runs out of output buffers.
SVT-AV1 4.0.x, 4.1.0 and 4.2.0 all have the bug; its fix, commit `ae2658e5`, is on SVT-AV1's master branch and in no release, and in interleaved runs under load it took 4.0.1 from 13 failures in 40 to none.
Under production threading the pinned image failed 10 of 740 runs, all 10 among the 420 that ran under CPU contention and none among the 320 that did not; the GOP does not cause it, since at two contended CPUs 5 of 150 runs failed under the floored GOP and 4 of 200 under the extended GOP.
The bug fails an attempt in one of two ways.
FFmpeg hangs at zero CPU while it keeps printing a progress line whose frame count no longer changes, and SIGTERM ended none of the hung runs; FFmpeg's source shows that `q` cannot end it either, because both only end FFmpeg's main loop, which then joins the stuck encoder thread, so only SIGKILL or a fourth signal ends the process.
Or the encoder emits packets out of decode order: FFmpeg rewrites each such packet's decode time to the previous one plus one tick, the output carries one-tick video samples, and the keyframes of 30 to 150 frames crowd into one or two intervals, so the producer fails with `SKIPPED_SEGMENT_NUMBER`, or FFmpeg exits 0 with segments that dav1d cannot decode, as 2 of the 10 pinned failures did.
Across the 1,170 runs a video sample shorter than half the nominal frame duration appeared only in runs where FFmpeg logged a non-monotonic decode time, and in every such run except one that hung before FFmpeg wrote the reordered fragment.
Of the encoder parameters the study ran, only `-svtav1-params lp=1` avoided the bug, in 90 runs without a failure, but it encodes 1080p at 0.78 times real time, too slow to stream; the study ran no other thread count, scene-change detection, lookahead, pinning or prediction structure.

`force_key_frames` chooses the frames it forces differently in its two modes (FFmpeg n8.1 `fftools/ffmpeg_enc.c`, `forced_kf_apply`).
An expression sees `t`, the time since the first frame that reached the encoder, and the counters `n`, `n_forced`, `prev_forced_n` and `prev_forced_t`, which count from that frame as well; no constant carries an absolute time, so no expression can name a point on the zero-based timeline.
A list compares each frame's own timestamp, which `-copyts -start_at_zero` places on the zero-based timeline whatever `-ss` is, with absolute times, and consumes at most one entry per frame.
Recordings of the recipe below under the pinned FFmpeg measured both modes for libx264 and SVT-AV1 at 23.976 and 29.97 fps with a 6 s period and the extended GOP, each encoder single-threaded so that runs compare frame for frame.
The sources were a constant-rate MP4 whose first frame is at zero, a variable-frame-rate MP4 whose first frame is at 41.7 ms, and an MPEG-TS source whose timestamps begin at 12 s and whose first output frame falls one frame after zero, each from the start and after seeks to 18, 30 and 42 s.
Under the expression, 202 of the 360 boundaries that a replacement attempt shares with the start-0 attempt began one or two frames apart (after a seek to 30 s on the constant-rate source, frames 1008, 1152, 1296 and 1440 against 1007, 1151, 1295 and 1439), and on the two sources whose first frame is after zero the start-0 attempt itself put 15 of 16 boundaries one frame after the first frame at or after the boundary.
Under a list of the boundaries from the attempt's start sequence number on, every attempt forced the first frame at or after each boundary, and none of the 372 shared boundaries differed.
A list that also held boundaries before the attempt's first frame forced one extra keyframe per such entry, on consecutive frames.
Past the list's last entry only the GOP places keyframes: segment starts drifted 47.7 ms per segment at 23.976 fps (39.4 ms at 29.97 fps), and the first skipped segment number came 126 segments after the last listed boundary (153 at 29.97 fps).
Linux caps a single argument to a process at 32 memory pages including its terminating zero byte: in the pinned image a 131,071-byte argument ran and a 131,072-byte one failed with "Argument list too long".
A list from zero that fits holds 22,771 boundaries at a 2 s period (12.65 h), 22,308 at 4 s (24.8 h) and 21,370 at 6 s (35.6 h), and FFmpeg parsed the longest of them in no measurable time; an empty list fails FFmpeg's option parsing ("Invalid keyframe time").
FFmpeg can also read an option's value from a file (`-/force_key_frames:0`), and it read a 1,000,000-entry list that way in about 150 ms.
The first junction of an encoded replacement attempt, where its first segment follows the start-0 attempt's segment before it, depends on where its first frame lands, not on its forced keyframes: with the seek at the boundary, 20 of the 36 recorded replacement attempts began one or two frames after the start-0 attempt's frame at that boundary, because the start-0 attempt fills a boundary of a variable-frame-rate source with a repeat of a source frame that precedes the seek, which the replacement attempt's accurate seek discards, and because the MPEG-TS demuxer lands on the next source keyframe after its target.
Seeking one period earlier and discarding that period as preroll aligned all 36 first junctions, and from each junction on the frames entering the encoder matched the start-0 attempt's frame for frame; a 1 s margin still dropped frames at all 12 MPEG-TS junctions, whose source keyframes are 2 s apart.
With the one-period seek, the preroll of each keyframe-verified encoder held only the keyframe that started the run.
At 15 of those junctions, with AAC encoded, the replacement attempt's first audio sample lay between 32 ms before and 10.7 ms after the end of the start-0 attempt's last audio sample, against 58.7 ms before to 58.7 ms after with the seek at the boundary.
The one-period seek recovers the start-0 attempt's frame only when the source holds a frame within the period before the boundary: on a variable-frame-rate MP4 with no frame between 23 s and 31 s (worker issue #44), the start-0 attempt filled segment 5 from 30.030 s with repeats of the frame at 23 s, while a replacement attempt that sought to 24 s discarded that frame and started segment 5 at 31.031 s, still inside that segment's interval.
In one recording of the same source, an input seek that keeps the frames before its target (`-noaccurate_seek`, which landed on the source keyframe at 22.022 s) passed the frame at 23 s to the encoder and forced segment 5's keyframe at 30.030 s, as the start-0 attempt did.

A real-player check of the server's fMP4 change (streamarr-server issue #433, recorded in its pull request) played media that the real server produced with two worker builds, one that sought an encoded replacement attempt to its first boundary and one built to this decision.
Each build produced 13 streams, covering H.264 stream copies and encodes with stereo and 5.1 AAC, a video encode with copied 5.1 AAC, and a variable-frame-rate source through libx264 and SVT-AV1, each uninterrupted and across a replacement attempt.
Each stream played three times in each of two scenarios, a seek harness and a linear playthrough, in Chromium 151 with hls.js 1.7.0, in WebKit 26.5 natively and in AVPlayer on macOS 26.6.2, beside the current worker's output of the same fixtures, MPEG-TS for H.264, as the control: 234 runs per stack.
With the seek at the boundary, the replacement attempt's AAC encoder started one priming frame before that boundary, while the start-0 attempt's segment ended with the audio that preceded the forced keyframe in decode order, so the junction held an audio gap of 32 ms on the 5.1 encode and 53.3 ms on the stereo encode.
MPEG-TS delivery masked such a gap twice: the MPEG-TS muxer shifted the start-0 attempt's timestamps but not the seek's, which turned the gap into an overlap, and hls.js remuxes MPEG-TS itself, dropping overlapping AAC frames and filling gaps with silent ones, whereas it appends fMP4 samples to Media Source Extensions unchanged.
Chromium buffers an audio gap longer than one AAC frame (21.3 ms at 48 kHz) as a hole, which hls.js seeks over, unless the track has already carried a longer AAC frame, as the stereo fixture's looped source had; on copies of one stream with the gap widened by hand, gaps of 26.7 to 42.7 ms also left playback stalled before its end.
That worker's H.264 encode with 5.1 AAC across a replacement attempt therefore passed 0 of 6 runs in Chromium, which skipped 0.125 to 0.167 s at the junction and stalled before the end, and it failed identically when the current worker's HLS muxer wrote the same variant as fMP4, while the MPEG-TS control passed 6 of 6 and WebKit and AVPlayer passed every run of it.
With the worker built to this decision, the one-period-early seek runs the replacement attempt's AAC encoder through the discarded period, so its frames at the junction fall within one AAC frame of the start-0 attempt's: the audio step at every encoded junction measured −16.0 to +5.3 ms, that cell passed 6 of 6, and video was continuous to the tick at every boundary.
Its stack and the control then each passed 210 of 234 runs, the same in every cell; the remaining failures are AV1 in WebKit and AVPlayer, which the test host's native decoders reject on both stacks, while Chromium played AV1 on both.
An iPhone Air (A19 Pro, Safari 26.6.1) then played each of the 11 H.264 streams from the worker built to this decision and from the MPEG-TS control once, natively and in hls.js 1.7.0 over `ManagedMediaSource`; with the two encodes across a replacement attempt from the worker that sought to the boundary, it passed all 48 runs.
In a separate check on the same iPhone, Safari played SVT-AV1 output natively, which the macOS host's native players had rejected, uninterrupted and across a replacement attempt, with the `av1C` box as the pinned runtime writes it and with that box corrected; the box's sequence header differs from the one SVT-AV1 puts in the stream (worker issue #43).
All 7 runs of that check passed, including Apple's own AV1 sample and two H.264 controls.
Every check used software encoders, and none measured a tvOS device or a hardware encoder.

The server side of the contract is narrower than the worker side assumes.
The server picks stream copy whenever the client supports the source video codec (`TranscodeDecisionService`); it computes an `align_keyframes_to_segments` flag, sends it, and neither the server nor the worker reads it, and no probe field carries keyframe spacing.
The server generates the media playlist from the probed duration alone: every `EXTINF` is exactly the configured period (6 s by default), the playlist defines segment `i` as media time `[i × period, (i + 1) × period)`, and the server never reads a per-segment duration from an upload.
Every start, seek, resume and replacement attempt seeks to segment index times period and passes the index as `start_sequence_number`; nothing handles a stream-copy seek that lands earlier.
The server sends the probed frame rate on every job attempt, and it sends 0 when the probe found no usable rate.
`SegmentUploadMetadata` carries `content_length_bytes` and the segment name, and no timing field.
The server caps a segment at 16 MiB, admits at most 8 concurrent uploads per worker and 64 MiB of buffered upload bytes fleet-wide, and reclaims an upload older than 120 s only when a later upload asks for admission.
The worker sends chunks in a plain `onNext` loop with no `isReady` check, starts its 5 s acknowledgement timer only after it has sent the last chunk, and sets no gRPC deadline; a stalled receiver therefore never times the worker out.
The worker advertises a slot count to the server and never enforces it.
The server frees an attempt's slot when it sends `StopVariant`, before the worker has stopped that attempt.
On the stop path, the worker sends `JobAttemptStopped` after `q` and a bounded wait, and on the forced-kill path it sends that message without waiting for the process to exit.
Worker issue #40 bounds those waits, the producer's memory and the slot count under the producer that this decision introduces.

ADR 0009 ties H.264 to MPEG-TS, and the server selects MPEG-TS for every codec except AV1 and HEVC.
H.264 in fMP4 has never been a production path; it appears only in hand-built test decisions that check command lines and decoding, never a player.
The server's fMP4 container accepts every audio codec its MPEG-TS container accepts plus FLAC, Opus and ALAC; Apple's authoring rules require fMP4 for HEVC, AV1, FLAC and ALAC and permit either container for H.264; and MPEG-TS needs a workaround for copying multichannel AAC that fMP4 does not need.
The playback proof of concept played H.264 with stereo and 5.1 AAC in multiplexed fMP4, uninterrupted and across a replacement attempt, in Chromium 151 with hls.js 1.7.0, in WebKit 26.5 natively and in AVPlayer on macOS 26.6.2; segments without a `styp` box played on all three.
A third proof of concept played segments built from bounded fragments, six `moof` boxes each with five beginning at non-sync samples plus a trailing audio-only fragment, served from the server's fixed-duration playlist form: on all three engines, in three repetitions of start, seek, play across a replacement attempt, seek near the end and natural end, they behaved exactly as today's one-`moof` HLS-muxer segments did, decoded to identical frames, and reused one byte-identical init across the replacement attempt.
The one failure, Chromium with hls.js stalling at 29.98 s after its junction with a replacement attempt, where the audio gap was 1536 ticks (32 ms), occurred identically with the HLS muxer's own packaging.
Chromium rejected the AC-3, E-AC-3 and HEVC codec strings before playback, and the test host's native players rejected AV1; those are decoder facts that the container does not change.
A single `moof` that carries a video `traf` and an audio `traf` is multiplexed fMP4; CMAF restricts a track to one media type, so the `cmaf` muxer flag does not make this output CMAF.
link:0019-recoverable-jit-hls-segment-delivery.adoc[ADR 0019] already decides that the worker retries a failed segment upload with bounded back-off; worker issue #1 (transferred from streamarr-server issue #253) tracks that work, and it has not landed.
ADR 0019 also rejects Low-Latency HLS blocking playlist reload as the way a client waits for a segment.
The moq-dev media layer consumes keyframe-aligned fragments read from FFmpeg's standard output, which is the unit this decision produces.

The playback capabilities epic (streamarr-server issue #399, with the vocabulary in ADR 0036) predates this decision and several of its sub-issues touch the same ground.
Issue #66 with worker issue #16 adds keyframe timestamps to the probe so that the server can derive copy-mode segment boundaries, advertise real durations and seek to a boundary keyframe; it currently describes those boundaries as "the way the HLS muxer cuts them", the relative rule that this decision found misaligns a stream-copy replacement attempt after a seek.
Issue #398 with worker issue #28 makes each worker advertise, per codec family, the encoder it will really use.
Issue #396 with worker issue #25 makes the video decision explicit and has the worker assert the produced output against it.
Worker issue #18 tags HEVC output `hvc1`, and worker issue #23 closes libx265's GOPs so that every forced keyframe is an IDR frame.
The epic also settles that every stream session is H.264-only until a client declares its codecs, so the container change in this decision reaches every stream session that exists today.
ADR 0036 distinguishes an attempt to dispatch a job from an attempt to try another format; a format attempt under issue #401 is a new variant with its own initialization segment, while a replacement attempt under ADR 0019 is the same variant and must keep its initialization segment.
The server's eligibility rule for replacement attempts considers only the source namespace today, and a worker chooses its encoder backend locally, so a replacement attempt under ADR 0019 can land on a worker whose H.264 encoder writes different parameter sets; in fMP4 those parameter sets live in the initialization segment, whereas MPEG-TS carried them in-band with every keyframe.

== Decision

=== One producer per job attempt reads FFmpeg's standard output

The worker runs FFmpeg under `ProcessBuilder` with the mp4 muxer writing to `pipe:1` and `-movflags cmaf+delay_moov+skip_trailer+frag_keyframe+frag_discont`.
The worker sets no `min_frag_duration`, so every keyframe starts a fragment, and it sets `-frag_duration` to a configured bound well below the segment period (measured at 1 s), so no fragment is longer than that bound; the worker, not the muxer, decides where a segment ends.
The worker forces a keyframe at every segment boundary for every encoder it runs, including SVT-AV1 and the hardware encoders that today receive only a frame-count GOP, disables scene-cut keyframes where it does today (libx264's `-sc_threshold`), and passes `-copyts -avoid_negative_ts disabled`.
libx265's scene cut belongs with its other GOP arguments to worker issue #23; a scene-cut keyframe only adds a second keyframe inside an interval, which the grouping below already joins to that interval's segment.
The worker forces the boundary keyframes with FFmpeg's list mode, never an expression: `-force_key_frames:0` receives the boundaries `k × period` in whole seconds for every `k` from the job's start sequence number up to, but not including, the media segment count that the server advertises for the variant, so every attempt forces the same frame at every boundary it shares with another attempt, the first frame at or after that boundary on the zero-based timeline.
The list starts at the start sequence number rather than at zero, because FFmpeg consumes at most one entry per frame and would force an extra keyframe for every entry before the attempt's first frame.
The server sends that count on every job attempt in `TranscodeExecution.media_segment_count`, a new, additive field number 5, and computes it with the same code that counts the media playlist's `EXTINF` entries: the probed duration in whole milliseconds divided by the period, rounded up.
Zero is not a valid count, and it is also what the worker reads from a server that omits the field.
The worker refuses a job whose count is zero, or whose start sequence number is not below its count, with `JOB_ATTEMPT_FAILURE_INVALID_SPECIFICATION`; it keeps no expression as a fallback, because Streamarr has no production instances.
In every mode such a job asks for a segment that the playlist does not advertise, and in an attempt that encodes video, under `VIDEO_TRANSCODE` or `FULL_TRANSCODE`, its list would be empty, which FFmpeg's option parsing rejects.
The worker ends the list early at the last entry that keeps the argument within 131,071 bytes, Linux's per-argument limit with 4 KiB pages; the Context gives the media length at which a list from zero reaches it.
The worker sets each encoder's frame-count GOP by whether that encoder is keyframe-verified.
An encoder is keyframe-verified when recordings of this recipe under the pinned FFmpeg (link:0029-pin-jellyfin-ffmpeg-releases.adoc[ADR 0029]), at a fractional frame rate from the start and after a seek, show three things: every forced keyframe comes out as a sync sample; the encoder restarts its GOP count there; and every forced keyframe, and the keyframe the GOP places when one forced keyframe is suppressed, starts a closed GOP.
A keyframe starts a closed GOP when no frame after it in decode order references a frame before it, and its picture type must guarantee that: an H.264 or HEVC IDR picture, or an AV1 key frame that is shown.
An HEVC CRA picture does not qualify even where no RASL picture follows it, because the encoder may place RASL pictures after any CRA picture, and the mp4 muxer marks a CRA picture as a sync sample either way.
libx264 and SVT-AV1 are keyframe-verified.
SVT-AV1 stays keyframe-verified although the pinned runtime's encoder library has upstream bug #2385 (worker issue #42), because that bug stalls or reorders the encoder's output rather than dropping a forced keyframe; the stall watchdog and the short-sample rule below turn each of its failures into a failed attempt that ADR 0019 replaces, and worker issue #42 pins a runtime with the fix.
libx265 is not keyframe-verified: under its default open GOP its forced keyframes are CRA pictures, and the keyframe its GOP placed for a suppressed one was followed by a RASL picture; it qualifies only once worker issue #23 closes its GOPs and a new recording shows every forced keyframe and that backstop as an IDR picture.
The floored GOP keeps a keyframe in every interval for libx265 but does not close its GOPs; worker issue #23 does.
No hardware encoder is keyframe-verified until worker issue #14 records it the same way when it brings that encoder under test.
A keyframe-verified encoder gets the extended GOP, the period times the probed frame rate rounded up, plus one frame, so its GOP never fires while forcing works, and if a forced keyframe were ever dropped the GOP would place a keyframe one or two frames after it, inside the same interval.
Any other encoder gets the floored GOP, the period times the probed frame rate rounded down, never up, so that even if it ignores the forced keyframe it still places one keyframe inside every interval and never skips a segment; most of its segments then hold two keyframes, and none holds none.
Past the list's last entry only the GOP places keyframes: the floored GOP still puts one in every interval, while the extended GOP drifts off the grid until the producer fails the attempt with `SKIPPED_SEGMENT_NUMBER`, and the replacement attempt that ADR 0019 dispatches from the skipped segment starts a new list there.
The worker adds `-start_at_zero`, so media time zero is the source container's start time, the earliest stream's first timestamp, for every attempt, whether it begins at the start or at a seek; the producer never shifts an attempt's timestamps on its own.
An attempt that encodes video and whose start sequence number is above zero seeks one period before its first segment, to the start sequence number minus one times the period, and the producer discards that period as preroll, exactly as it discards a stream copy's.
When the period before the first boundary holds a source frame, and on an MPEG-TS source a source keyframe, the attempt then holds the same frames as the start-0 attempt from that boundary on, where a seek to the boundary itself can lose the frame that the start-0 attempt repeats at the boundary and can land late on an MPEG-TS source.
Otherwise the attempt's first segment starts late inside its interval, which the producer cannot tell from a correct start; the deferred list below names that case.
Under the extended GOP the preroll holds only the keyframe that starts the attempt's output, because the list starts at the start sequence number and that GOP is longer than one period; the floored GOP may add one more keyframe, which the producer discards with the rest of the preroll.
The worker computes this seek from the start sequence number and the period, so it needs no contract change, and the server still sends the boundary as the seek position.
When it copies AAC audio, the worker adds `-bsf:a aac_adtstoasc`, because `delay_moov` stops the mp4 muxer from inserting that filter on its own; the filter converts the ADTS frames of MPEG-TS sources, which otherwise fail the copy, and passes raw AAC from MP4 and MKV sources through byte for byte.
When an attempt encodes video, the worker passes `-r:v:0` with the probed frame rate, the same number from which it computes its frame-count GOP, and sets no `-fps_mode`.
FFmpeg's automatic mode then takes the constant-rate path and, under `-copyts`, the internal variant that keeps the first timestamp, so the pipe carries the same constant-rate frames the HLS muxer produced, keyframes from frame-count encoders land on the period, and a seek does not pad from zero.
A recorded fixture pins that a seek under these flags starts at the seek point, so an FFmpeg upgrade cannot silently change the selection.
The worker refuses a job that encodes video with `JOB_ATTEMPT_FAILURE_INVALID_SPECIFICATION` when its probed frame rate is not positive and finite, because it can neither pass that rate to `-r:v:0` nor count a GOP from it; streamarr-server issue #446 has the server refuse a stream session that would encode video without a usable frame rate before it dispatches any job attempt, instead of spending ADR 0019's recovery on a refusal that repeats.
The worker change removes the HLS muxer, the output directory, the predicted file names, the directory poll and the liveness poll.
The server keeps generating every playlist (ADR 0008).
A producer module in the worker's `engine` package owns the FFmpeg process, reads its standard output, and observes its exit.
The producer imports no gRPC protocol types.
The producer parses top-level ISOBMFF boxes: `ftyp` and `moov` form the initialization segment, and each `moof` with the `mdat` that follows it forms one fragment.
The producer validates every box size against the server's segment cap before it allocates memory for the box, and it fails the attempt on a box that exceeds the cap.
It also fails the attempt when a `trun`'s samples, placed where its data offset puts them, do not lie inside the body of the `mdat` that follows its `moof`, because the producer delivers each fragment as that pair of boxes and a sample outside them would not travel with its fragment.
When a fragment carries a video track, the producer reads the fragment's start on the zero-based timeline as the presentation time of that track's first sample (`tfdt` plus that sample's composition offset from `trun`, in the track's timescale, with no edit list applied) and whether that sample is a sync sample (from the `trun` first-sample flags or the `tfhd` and `trex` defaults).
A fragment without a video track has no start and no sync status of its own; every audio sample is a sync sample, and an audio sync flag never opens a segment.
The producer never compares a raw `tfdt` with the nominal grid, and it never computes a fragment's end from its sample durations.
In an attempt that encodes video, the producer fails the attempt with the named reason `SHORT_VIDEO_SAMPLE` when a video sample's duration, from `trun` or the `tfhd` and `trex` defaults, is shorter than half the nominal frame duration at the probed frame rate: such a sample marks a packet whose decode time FFmpeg rewrote after the encoder emitted it out of order, and the attempt's segments may then fail to decode even though FFmpeg exits 0.
A stream copy is exempt, because a variable-frame-rate source's own sample durations vary.
Unlike a skipped segment number, a short sample does not close the open segment: the fragment that holds the sample would join or close that segment, and the producer does not judge which samples of an out-of-order output still decode, so the replacement attempt encodes that segment again.

=== A segment is the group of fragments that start inside its interval

Segment `N` covers media time `[N × period, (N + 1) × period)` on the zero-based timeline, exactly as the server's playlist defines it, and that timeline is the same for the initial attempt and for every replacement attempt.
This partition is the one boundary rule for the whole system: segment `N` begins at the first keyframe whose presentation time lies inside its interval, and a stream copy cannot produce a segment whose interval holds no keyframe.
Issue #66 applies the same rule on the server from probed keyframe timestamps, which lets the playlist advertise each copy-mode segment's real duration and lets a copy seek land on the segment's own first keyframe; that work replaces its "the way the HLS muxer cuts them" wording with this rule, and until it lands the server advertises fixed durations and the producer discards preroll as described below.
A fragment that begins with a sync sample belongs to the segment whose interval contains its start; a fragment that begins with a non-sync sample, or that carries no video track at all, always joins the open segment, because a segment can only begin at a video keyframe.
Such fragments appear only after the last video sample, one per fragmentation target of audio that outlasts the video, and the closed last segment keeps them; if one ever arrived before the producer had opened any segment, that fragment would wait and join the first segment that opens.
The producer concatenates the fragments of one segment, byte for byte and in arrival order, into one media segment.
The producer closes the open segment when it receives a sync-first fragment whose start reaches the next segment's start, and only then.
`-frag_duration` is a fragmentation target that measurably shortens that lag, not a guarantee: the muxer cuts at the first packet after the target (measured spans of 1.001 s for H.264 and 1.033 s for SVT-AV1 at a 1 s target), so the closing fragment follows the boundary by about one target in media time rather than one whole segment, and wall-clock delivery further depends on encoder speed and on back-pressure from the server.
End of file closes the last open segment.
The producer discards a fragment whose segment number is below the job's start sequence number; that fragment is the preroll of a stream-copy seek or of an encoded attempt's one-period-early seek, and its segment belongs to another attempt.
The producer fails the attempt with the named reason `SKIPPED_SEGMENT_NUMBER` when a sync-first fragment skips a segment number, because the attempt's output then holds no keyframe inside the skipped segment's interval, so this attempt can never deliver that segment.
In a stream copy the source has no keyframe there, so no attempt can produce the segment; an attempt that encodes video skips a number only when its keyframes leave the grid, past the end of its list or through an encoder fault, and the replacement attempt from the skipped segment forces a keyframe there.
The producer detects the skip at that fragment: it admits nothing from it on and reads nothing after it into a segment.
The same fragment is the boundary that closes the open segment, so the producer closes that segment and delivers it as at any other boundary before it records the failure.
It is the only segment the producer closes after detecting the skip, and all of its content precedes the skipping fragment, so it is complete and correctly numbered; dropping it would only defer it to a replacement attempt, which in a stream copy reproduces the same bytes and fails at the same fragment, and after an encode repeats the work of encoding it.
A segment that runs past the end of its interval is not itself a failure; whether it conforms to the duration the playlist advertises is issue #66's policy, not the producer's.
The producer assembles one segment in memory, bounded by the server's segment cap, and starts the upload only after it closes the segment, so the upload contract does not change: the worker sends the metadata message with `content_length_bytes` first, then the chunks, and the server acknowledges the segment.
The worker sends the next chunk only when the gRPC stream reports `isReady`.
The reader admits each box only while its bytes, with everything the producer already holds, fit inside the producer's budget of two segment caps, and it hands over a closed segment only once the server has acknowledged the previous one; otherwise the reader stops reading and the pipe blocks FFmpeg.
This back-pressure loses nothing: the worker never skips or drops a fragment.
MoQ's importer skips no fragment either: it publishes every fragment of the tracks it publishes or fails, and skipping a stale or missing group is the behaviour of its live consumer; the worker keeps every fragment because the server has already advertised each segment to on-demand clients.

=== The attempt owns completion, stop and stall

An attempt completes only when four facts hold, in whatever order they arrive: the pipe reached end of file on a box boundary with no `moof` still waiting for its `mdat`; FFmpeg exited with status 0; the server acknowledged every segment the producer closed, with no segment number skipped; and the attempt is still active.
The producer fails the attempt when the pipe ends inside a box header, inside a box body, or after a `moof` with no `mdat`; when FFmpeg exits with a non-zero status; or when the server has not acknowledged every closed segment.
An exit with status 0 and no media segment also fails the attempt, which matches today's behaviour.
When the producer fails the attempt on FFmpeg's output itself, because that output is truncated, malformed or over the segment cap, skips a segment number or holds a short video sample, or because reading the pipe fails, it lets the complete segment already awaiting acknowledgement finish before it records the failure, and a stop that arrives meanwhile takes over; the producer records a failed upload, a stall or an unexpected error at once and cancels any upload in flight.
A completed attempt means that FFmpeg finished cleanly and the server accepted everything FFmpeg produced; it does not mean that the attempt covered the advertised timeline.
The worker uses the media segment count that the server advertises for two things only, to refuse a job that asks for no advertised segment and to end its list of forced keyframes; it neither checks that an attempt reached the last segment nor fails an attempt that ended before it, so the server owns coverage: under ADR 0019 the server's delivery coordinator treats an advertised segment that no attempt produced as a request-driven recovery, and it, not the worker, tolerates the rounding between the probed duration and the last segment.
The worker settles an attempt's outcome once, under its own monitor, and a stop that is already in progress prevents the worker from also completing or failing the attempt.

A stop ends the attempt for good, and it changes what the producer admits, not what the server has already committed.
When the worker stops an attempt, the producer records the stop under its own lock, then writes `q` to FFmpeg and cancels any upload in flight.
The reader then drains the pipe to end of file and discards everything it reads, including the partial fragment FFmpeg flushes when it receives `q`.
The producer delivers nothing after a stop.
Cancelling an upload is not a rollback: the server may already have committed the cancelled segment, and the server's attempt fence at commit time decides whether the server publishes that segment (ADR 0019).
The attempt keeps whichever outcome the producer records first: the worker never reports a failure that the producer observes after the stop, and when the producer recorded a completion or a failure before the stop, the worker answers `StopVariant` with that outcome instead of `JobAttemptStopped`, unless it has already reported the outcome, in which case the stop draws no further message.
After writing `q`, the worker waits for FFmpeg to exit within the grace period and then destroys the process forcibly with SIGKILL, as it does today, and it waits again for the exit after the forced kill.
The worker sends `JobAttemptStopped` only after it has observed the process exit, on both paths, so that message always means the process is gone.
The producer settles every outcome only after FFmpeg has exited and no upload is left in flight, so no upload follows an attempt's report.
A stop waits for that exit without holding the worker's control stream, so the worker keeps each job attempt's messages in order but not the messages of different attempts: a replacement attempt's `JobAttemptStarted` can reach the server before the `JobAttemptStopped` of the attempt it replaces.
Draining the pipe is what lets FFmpeg finish its flush inside the grace period; a pipe nobody reads would otherwise hold FFmpeg until the forced kill.

Three waits bound an attempt's progress, and each is separately configured and cancellable by a stop.
An encoder-stall watchdog fails the attempt when no byte has arrived from the pipe within the stall threshold while the reader was reading; a blocking pipe read cannot time out on its own.
The watchdog measures liveness only by bytes on standard output, never by standard error or by the process being alive, because a hung FFmpeg keeps printing its progress line.
When the watchdog fires, the worker sends FFmpeg SIGTERM, destroys it forcibly with SIGKILL once the stop's grace period passes, and waits for the exit, because a hung FFmpeg exits on neither `q` nor SIGTERM.
A readiness wait fails the upload when the gRPC stream has not reported `isReady` within its bound.
An acknowledgement deadline on each upload call fails the upload when the server has not acknowledged the segment within its bound, counted from the first byte sent, not from the last.
Back-pressure from the server therefore pauses the encoder-stall watchdog but never exempts the attempt from every bound.
The producer never holds its lock across a pipe read, a process wait, an upload or a call into the worker.

=== A producer and a worker each admit memory within a budget

Each producer holds at most two segment caps of FFmpeg's output plus one fixed scratch buffer for box headers; it reserves each box's bytes against that budget before it allocates the box, and it never holds a whole fragment twice.
The budget counts bytes rather than segments, because at a boundary the reader holds the segment it has just closed and the fragment that closed it while the previous segment still awaits acknowledgement, and together they still fit within two caps.
The worker enforces the slot count it advertises: a job that arrives while every slot holds an active job attempt fails with `JOB_ATTEMPT_FAILURE_STARTUP_FAILED` before FFmpeg starts, because that value says this worker could not start the attempt, where `JOB_ATTEMPT_FAILURE_INVALID_SPECIFICATION` would say that no worker could run the job; the contract has no failure value for capacity.
Only active job attempts count against the slots: the server also counts probes against them when it dispatches, but here the slots bound segment memory, and a probe holds none.
A stopping attempt does not count against the slots: the stop cancels its upload in flight and its producer discards everything it reads after the stop, so the producer admits no segment memory, and the server has already freed that slot when it sent `StopVariant`.
The worker's segment memory stays within one worker-wide budget of the slot count times two segment caps, which charges every producer, a stopping producer included, until that producer has released its segments.
A stopping producer releases the segment it was assembling at the stop, because it will never deliver it, and the segment in delivery as soon as the cancelled upload returns, without waiting for FFmpeg to exit.
A job that takes a stopping attempt's slot can therefore wait for memory, its reader paused and its FFmpeg blocked on the pipe, but only until those cancellations return, never for another attempt's FFmpeg to exit.
FFmpeg's own memory, the kernel pipe buffer and gRPC's flow-control window sit outside that budget; the container runtime bounds them, and this decision does not claim to.

=== Retry follows the same producer interface

Until worker issue #1 lands, a failed upload fails the attempt, and the server dispatches a replacement attempt within the budget ADR 0019 sets.
Once worker issue #1 lands, the producer keeps the closed segment it already holds until the server acknowledges it, and retries the upload with bounded back-off against a server that accepts a re-sent segment idempotently.
That retry sits behind the same producer interface and needs no new buffer.

=== Tests substitute the process

Tests substitute the process, not the producer: production uses `ProcessBuilder`, and the worker-session tests supply a scripted `Process`, so the real producer and its grouping and ordering rules run in every test.
Recorded fixtures pin the zero-based grid rule for these cases:

* a constant 23.976 fps encode;
* a stream copy with irregular keyframes;
* a variable-frame-rate encode;
* a variable-frame-rate stream copy with B-frames;
* an irregular variable-frame-rate source through SVT-AV1 under the list and the extended GOP, with and without a seek;
* a source whose timestamps begin after zero;
* an attempt whose output ends in an audio-only fragment;
* a stream-copy replacement attempt whose preroll the producer discards;
* a stream copy whose keyframe gap exceeds the period and that delivers its complete segments before it fails with `SKIPPED_SEGMENT_NUMBER`;
* each software encoder with every forced keyframe and with one suppressed, with the picture that starts each keyframe;
* a start-0 encode of a source whose first frame is after zero, with every forced keyframe on the first frame at or after its boundary;
* an encoded replacement attempt whose forced keyframes land on the start-0 attempt's frames and whose one-period preroll the producer discards;
* the init segment's byte identity between a start-0 attempt and an attempt that seeks.

The recorder reads the picture that starts each keyframe, the NAL unit type for H.264 and HEVC and the frame header for AV1, so the fixtures pin libx264's IDR pictures, SVT-AV1's shown key frames and libx265's CRA pictures, and both the recorder and CI's check fail when a keyframe of a keyframe-verified encoder does not start a closed GOP.
For every fixture the recorder also runs the HLS muxer on the same source and records each segment whose cut differs from the grid, with its cause: the HLS muxer measures from the run's first packet, not from zero, and in the pinned FFmpeg that reference can be an audio packet's time read in video ticks.
The HLS muxer's cuts are a comparison, not the reference.
CI re-derives every recording's expectations from the committed recordings, without FFmpeg, and fails when a stated expectation differs from what the recording's bytes decide.
Smoke and image tests keep real FFmpeg, and a smoke test runs more than 1000 one-second segments through each software encoder to show that no interval is ever skipped, SVT-AV1 at a parallelism of one so that upstream bug #2385 cannot fail it.
The server's scripted FFmpeg test fixture writes a recorded fMP4 stream to standard output instead of writing segment files.

=== HLS media segments are fMP4 only

The container no longer follows the codec: the server serves every variant as multiplexed fragmented MP4 in an HLS version 6 playlist with an `EXT-X-MAP` tag.
The output keeps the `cmaf` muxer flag as part of the measured recipe and claims no CMAF conformance; strict CMAF, which would need one media type per track, stays outside this contract.
H.264 moves from MPEG-TS to fMP4.
The server change removes the codec-to-container rule, the `.ts` code paths, the MPEG-TS audio rules and the multichannel-AAC copy workaround.
The 10 s timestamp offset that `-max_delay` causes in MPEG-TS output disappears with the container.
Every attempt of a variant produces the same initialization segment, so the server keeps serving the init it already holds to every later attempt's segments, as ADR 0019 assumes; a recorded fixture pins byte identity of the init across a start-0 attempt and an attempt that seeks, for encode and for stream copy.
That identity holds only within one encoder backend, so a replacement attempt under ADR 0019 must run on the same backend as the attempt that produced the init: the first attempt pins the backend on the variant, the server dispatches replacement attempts only to workers that advertise the same encoder for that codec family (issue #398 with worker issue #28), and the server rejects a replacement attempt whose uploaded init differs from the stored one with a named failure instead of serving it.
The init comparison needs no contract change and lands with this decision; the backend affinity lands with the encoder capability.
The `hvc1` tag from worker issue #18 applies to the mp4 muxer unchanged, and the producer's parser reads the sample entry from `moov`, which is where worker issue #25's assertion of the produced output against the video decision belongs.
The worker rejects a job that requests `CONTAINER_FORMAT_MPEG_TS` and reports `JOB_ATTEMPT_FAILURE_INVALID_SPECIFICATION`.
The container stops being a decision: the server's `ContainerFormat` type and the worker's counterpart go away with their second value, and the segment extension, the playlist version, the `EXT-X-MAP` tag and the audio codec set become facts of HLS delivery rather than a choice per variant; the explicit video decision of issue #396 carries no container.
The worker contract keeps its container field, because ADR 0033 keeps the contract additive; the server always sends fMP4, and a future container would be a new decision, not a revived enum value.
The server pull request proves the container flip against real players before it merges: H.264 with stereo and 5.1 AAC, uninterrupted and across a replacement attempt, in the web player's hls.js, in WebKit natively and in AVPlayer, with the current worker's MPEG-TS output as the control, and the fMP4 stack passes every cell that the control passes.
The control is MPEG-TS rather than the HLS muxer writing fMP4, because MPEG-TS is what H.264 stream sessions play today and it hid an audio gap at a junction that fMP4 exposes.
That check includes segments whose inner fragments begin with non-sync samples, which the `-frag_duration` target produces, and a variable-frame-rate source encoded with SVT-AV1.
Codec eligibility stays a client capability decision; choosing fMP4 removes a packaging branch and adds no decoder.
Direct play never touches the worker, so this decision does not affect it.

=== The delivered unit is a keyframe fragment

The producer delivers the initialization segment first, then each fragment with the tracks it carries and, when it carries video, its sync-sample flag and its start; a fragment may carry audio alone.
The HLS adapter groups fragments into segments as this decision describes and uploads each segment once.
A future MoQ adapter would run FFmpeg with `separate_moof` and a much smaller fragment bound, as the moq-dev demo does, and publish each fragment as a frame within a group, behind the same producer interface.
This ADR does not adopt MoQ, its transport or its live retention policy.

=== Rollout

The worker change and the server change land as separate pull requests.
Before either, a server pull request adds `media_segment_count` to `TranscodeExecution` and sends it on every job attempt; the current worker ignores the new field, and the new worker refuses a job without it.
The worker pull request merges first.
The server pull request then merges the container switch, the worker image pin bump, the test fixture change and the player check together.
Between the two merges, a server built from `main` fails H.264 jobs against the new worker image.
Streamarr has no production instances, so this decision accepts that window.
Worker PR #38 remains the interim fix for issue #36 until the producer lands.

Explicitly deferred:

* A remux gate on keyframe spacing (issue #66 with worker issue #16).
The server still chooses stream copy without reading the source's keyframe interval, and `align_keyframes_to_segments` is still unread; a source whose keyframe gap exceeds the period delivers its segments up to the gap and then fails under this producer with `SKIPPED_SEGMENT_NUMBER` instead of producing misnumbered segments, until the probed keyframe timestamps let the server refuse the copy up front.
Whether a copy segment longer than the period, such as each 10.01 s segment before that gap, conforms to the advertised duration is also issue #66's policy.
* Encoder-backend affinity for ADR 0019 replacement attempts (issue #398 with worker issue #28); the init comparison above stands alone until then.
* Low-Latency HLS parts and a part-upload contract.
A part's length stays unknown until the part ends, and ADR 0019 rejects LL-HLS blocking reload as the way a client waits for a segment.
* MoQ transport and publication.
* Retention-based retry (worker issue #1), which follows later behind the same producer interface and needs the server to accept a re-sent segment idempotently.
* A check of the audio where a replacement attempt's first segment joins the previous attempt's last.
The one-period-early seek kept every measured gap there under one AAC frame, but nothing measures or corrects it, so a source, sample rate or encoder that left a longer gap would again become a hole that Chromium stalls on, now that fMP4 reaches Media Source Extensions without the MPEG-TS remux that hid it.
* Playback on tvOS devices, and of output from hardware encoders; the player checks ran on one macOS host and one iPhone, with software encoders only.
* A runtime whose SVT-AV1 fixes upstream bug #2385 (worker issue #42).
The worker takes FFmpeg as a pinned prebuilt release under ADR 0029, and no SVT-AV1 release contains commit `ae2658e5` yet.
* A seek that reaches the start-0 attempt's first frame whatever the source's frame spacing.
The one-period-early seek of an encoded replacement attempt starts its first segment late when that period holds no source frame, as on a variable-frame-rate source with a longer gap between frames, and leaves a video gap at the junction.
On an MPEG-TS source it also starts late when that period holds no source keyframe, because FFmpeg's MPEG-TS demuxer lands on the next keyframe after the target; the producer accepts either start because it lies inside the segment's interval.
A stream-copy replacement attempt of an MPEG-TS source still starts its video up to one keyframe interval late while its audio starts on time, exactly as today.
A seek that keeps the source frames before its target and discards them as preroll, such as `-noaccurate_seek` on MP4 sources or a two-stage seek on MPEG-TS sources, is worker issue #44's follow-up, with a recorded fixture that holds a gap between frames longer than the period.
* Init identity across FFmpeg builds.
The init carries the muxer's version string, so a stream session that outlives a worker image upgrade could receive a replacement attempt whose init differs from the one the server serves; nothing measured that case.
* Forced keyframes past the end of the list.
A media file longer than its list covers drifts after the list ends, and the junction where the replacement attempt starts a new list repeats about one period of media, which ADR 0019's recovery cannot withdraw once the server has published it.
A rollover that ends each attempt at its list's last boundary and leaves the rest to a replacement attempt with a new list would avoid that repeat at the cost of a request-driven replacement attempt; reading the list from a file stays rejected below.
* A production memory bound.
The proofs of concept measured pacing and one retention budget; this decision obliges the per-producer and worker-wide budgets above, and nothing has measured them.

== Consequences

* One ordered channel replaces two side channels, so the race between the directory and the exit cannot occur, and the worker needs no step that rescans, drains or reconciles after exit.
* The worker gains an ISOBMFF box parser, a fragment-to-segment grouper on the absolute timeline and strict end-of-file rules, each with its own tests, and loses `awaitSegment`, the liveness poll, the 50 ms constant and the output directory.
* The worker, not FFmpeg's muxer, now decides where a segment ends, so an FFmpeg upgrade under link:0029-pin-jellyfin-ffmpeg-releases.adoc[ADR 0029] cannot silently merge or split segments; the recorded fixtures pin that rule.
* A stream-copy replacement attempt now starts on the advertised segment boundary because the producer discards the preroll fragment; today's HLS muxer starts it one keyframe early, which the server never noticed because it reads no segment timing.
* An encoded replacement attempt starts its first segment on the start-0 attempt's frame whenever the period before that segment holds a source frame, and the audio at that junction left no gap as long as one AAC frame in the recordings or the player check, so Chromium no longer buffers a hole there; in exchange it encodes one period that the producer discards, and nothing has measured what that adds to the time before a replacement attempt's first segment on real content.
* A segment closes about one fragmentation target after its boundary in media time, where today's rename happens at the boundary itself; the target is configuration, and each segment carries about period-over-target `moof` boxes instead of one.
* Each producer holds up to two segment caps in memory, the segment awaiting acknowledgement and the next one assembling, where today one closed segment waits on disk; the worker holds at most the slot count times two segment caps, and a stop releases a producer's share without waiting for FFmpeg to exit.
* Variable-frame-rate sources are still padded to a constant rate, as today, but the rate is now the worker's own probed number rather than FFmpeg's guess, so the frame-count GOP and the output rate cannot disagree.
* Every encoder receives time-based keyframes, so segment boundaries stop drifting with the GOP.
A keyframe-verified encoder's extended GOP never fires, so each of its segments holds one keyframe instead of the two that most segments hold under the floored GOP; any other encoder keeps the floored GOP and its extra keyframe until it is keyframe-verified, libx265 after worker issue #23 and each hardware encoder under worker issue #14.
* The extended GOP alone no longer guarantees a keyframe in every interval: if the encoder stopped honouring forced keyframes altogether, its GOP would drift off the grid until an interval held no keyframe, as it does past the end of the list.
The producer then fails that attempt with `SKIPPED_SEGMENT_NUMBER` instead of misnumbering segments, and the keyframe verification recordings exist to catch that regression when FFmpeg is updated under ADR 0029.
* Under the extended GOP the forced keyframes alone place every boundary that the list holds, and the list puts each one on the first frame at or after its boundary in every attempt, so a junction between two attempts at any listed boundary after a replacement attempt's first neither drops nor repeats a frame.
The expression, which measured from each attempt's first frame, had put shared boundaries one or two frames apart, and the floored GOP's extra keyframe had hidden that difference.
* The worker contract gains `TranscodeExecution.media_segment_count`, the count the playlist advertises, and the worker uses it only to refuse a job that asks for no advertised segment and to end its list; coverage stays the server's under ADR 0019.
* An FFmpeg command line carries up to 131,071 bytes of keyframe times, one argument that FFmpeg parses in no measurable time.
* SVT-AV1 attempts in the pinned runtime still fail under CPU contention, as the study found; the stall watchdog and the short-sample rule turn a hung FFmpeg or undecodable segments into a failed attempt that ADR 0019 replaces, and both rules stay after worker issue #42 pins a fixed runtime, because they cover every encoder.
* A media file longer than its list covers drifts after the list ends, and through a keyframe-verified encoder its attempt fails with `SKIPPED_SEGMENT_NUMBER`; the replacement attempt starts a new list on the grid, and its first segment repeats about one period of media that the last drifted segment already held.
* The server pins a replacement attempt under ADR 0019 to the encoder backend that produced the variant's init and refuses a differing init with a named failure; the H.264 flip removes the in-band parameter sets that MPEG-TS carried, and this pinning replaces them.
* The upload loop gains `isReady` flow control, a readiness bound and an acknowledgement deadline, none of which it has today, and the worker starts enforcing the slot count it advertises, counting only active attempts.
* The worker no longer writes media to disk, so it no longer needs a segment base path or the code that cleans that path up.
* AAC stream copies gain an explicit `aac_adtstoasc` filter that the HLS muxer inserted on its own; without it every MPEG-TS source with AAC fails to copy under the new muxer flags.
* MPEG-TS disappears end to end, and so does the container as a type: the server change deletes `ContainerFormat` and the worker change deletes its counterpart, rather than keeping either with one value, and ADR 0036's list of names to keep drops `ContainerFormat`; H.264 playback changes container, and the server pull request carries the player evidence for that change.
* H.264 in hls.js loses the MPEG-TS remux that hid timestamp gaps: an audio gap longer than one AAC frame at a junction now reaches Chromium as a hole, so the producer's alignment of a replacement attempt, not the player, keeps the junction continuous.
* The server and worker repositories change in lockstep once; the container switch, the pin bump, the scripted-FFmpeg fixture and the player check form one server change.
* A stop can no longer end in a forced kill because the reader sat blocked in an upload; the producer must drain the pipe and cancel the upload, and neither step is optional.
* A MoQ proof of concept needs `separate_moof`, a smaller fragment bound and a publisher adapter, not a second producer.

== Rejected Alternatives

* A producer module that watches the output directory with inotify and treats exit as an event removes the race by ordering the two facts, but it keeps two channels, needs a polling fallback on macOS, and the pipe would replace it anyway.
* A pull-style producer with a `next()` method leaves the read loop with the caller, and a fake producer would have to duplicate the ordering rules.
* Exit and segment-arrival callbacks on the existing process manager leave the stop logic in the worker under a different lock, so the worker would still upload a segment that FFmpeg flushed during a stop.
* The first proof of concept measured a loopback HTTP receiver inside the worker, the design that several live-streaming servers use, and found that FFmpeg ignores the response status, never re-sends, exits with status 0 whatever the receiver did, and exits before a slow receiver has read its output.
The receiver would have to rebuild every delivery guarantee, and the worker would gain a listening port that ADR 0033 reserves for health checks.
* FFmpeg `PUT`ing segments straight to the server adds a second transport beside gRPC, fences attempts by a URL token instead of by connection ownership, moves TLS into FFmpeg's HTTP client, and offers no per-segment retry.
* Having the server read segments from storage the worker shares moves the same two-channel race onto the server and across a network filesystem, and ADR 0018 and ADR 0019 already defer that design.
* Letting the mp4 muxer cut one fragment per segment with `-min_frag_duration` just under the period matched the HLS muxer only at a constant frame rate with keyframes exactly one period apart; it produced 7 fragments for 11 segments on an irregular stream copy and 9 for 11 on a variable-frame-rate encode, and no fixed minimum can hold for arbitrary keyframe spacing.
* Setting `-min_frag_duration` to exactly the segment period merged two segments at 23.976 fps in the first proof of concept.
* Checking each fragment's raw `tfdt` against segment number times period rejected 23 of 30 valid segment starts, because decode time carries composition offsets, a 144-frame period is 6.006 s, and the first audio `tfdt` wraps negative.
* Keeping the preroll fragment of a stream-copy replacement attempt inside its first segment reproduces today's behaviour, where that attempt's segments overlap the previous attempt by one keyframe interval.
* Seeking an encoded replacement attempt exactly to its first boundary, which leaves nothing to discard, drops frames at the first junction on variable-frame-rate and MPEG-TS sources and left an audio gap there that Chromium with hls.js stalled on, and a 1 s margin still dropped frames on MPEG-TS sources whose keyframes are 2 s apart; one period is the unit the producer already discards as preroll.
* Failing the attempt before closing the open segment when a fragment skips a segment number discards a complete segment whose number is correct: in a stream copy every replacement attempt that starts at that segment reproduces the same bytes and fails at the same fragment, so no attempt could ever deliver it, and in an encode a replacement attempt would encode it again.
* Pinning the recorded fixtures to the HLS muxer's cut points would pin its first-packet origin, which misaligns a stream-copy replacement attempt, and a reference that the pinned FFmpeg reads in the wrong time base and that FFmpeg's development branch has already changed.
* Closing a segment on a fragment's computed end, its start plus its sample durations, closed a segment before its last fragment arrived on a variable-frame-rate stream copy, where a fragment starting at 6.0 s summed to 12.0 s while the next fragment started at 11.9 s.
* Cutting a fragment at every frame, so that the closing signal arrives within one frame, multiplies the `moof` count by the frame rate for a gain that the bounded `-frag_duration` already delivers.
* Passing `-fps_mode:v:0 cfr` to keep the HLS muxer's constant-rate behaviour padded 720 duplicate frames from time zero after a seek to 30 s; the HLS muxer's behaviour is an internal variant that `-copyts` selects and that the command line cannot request.
* Leaving frame synchronization to the mp4 muxer's default, or passing `-fps_mode:v:0 vfr` for every encoder, keeps each source frame's own timing, which suits encoders that take time-based forced keyframes but stretches a frame-count GOP: SVT-AV1 on an irregular variable-frame-rate source placed keyframes about ten seconds apart and left five of eleven segment numbers without a keyframe.
* Choosing synchronization per encoder, `vfr` for time-based keyframes and a forced rate for frame-count GOPs, works in measurement but adds a second rule for no gain over one forced rate that reproduces today's output for every encoder.
* Keeping the frame-count GOP rounded up as the only source of keyframes, as the worker computes it today for SVT-AV1 and the hardware encoders, lets the keyframes drift until an interval holds none, and letting a replacement attempt heal that costs a stall each time; the extended GOP is safe only because its forced keyframes, not the GOP, place every boundary.
* Keeping the floored GOP for every encoder, keyframe-verified or not, adds a second keyframe and a one-frame fragment beside most boundaries to guard against a failure that the keyframe verification recordings rule out and that `SKIPPED_SEGMENT_NUMBER` would still name.
* Verifying an encoder's keyframes on its sync samples alone would admit libx265, whose forced keyframes are CRA sync samples and whose backstop keyframe carried a RASL picture that references the previous GOP; the sync flag cannot tell an open GOP from a closed one.
* A GOP of exactly the rounded-up number of frames fires on the same frame as every forced keyframe that follows a gap of that length, so a recording cannot show whether the encoder honoured the forced keyframe or reached its GOP limit.
* Keeping the expression `gte(t,n_forced*period)` measures every boundary from the attempt's first frame, and no expression can do otherwise, because FFmpeg gives an expression no absolute time; it put boundaries that a replacement attempt shares with the start-0 attempt one or two frames apart, and on sources whose first frame is after zero it put the start-0 attempt's own boundaries one frame late.
* Anchoring the expression with a predicted first frame, `gte(t+T0,(n_forced+k)*period)` with `T0` the first output frame at or after the seek position and `k` the start sequence number, aligned the constant-rate source whose first frame is at zero but left 80 of 360 shared boundaries one frame apart on the variable-frame-rate and MPEG-TS sources; the first frame's time depends on the source's frame times and on where the demuxer lands, which the worker cannot know before FFmpeg runs.
* Measuring each forced keyframe from the previous one, `gte(t,prev_forced_t+period)`, is a frame-count GOP in another form: from the start it drifted one frame every seven segments at 23.976 fps, a replacement attempt from 42 s landed one frame off the start-0 attempt at every later boundary, and like the rounded-up GOP it would skip a segment after about 1000.
* A fixed horizon, the same number of boundaries for every job whatever the media's length, works without a contract change, since a list that ran past the media's end produced byte-identical output, but the server already owns the grid and the count it advertises, and a new contract field costs little before production.
* Having the worker probe the source's duration before each attempt to end the list adds a process and its latency to every start and replacement attempt, and can disagree with the probe from which the server advertises the count.
* Reading the list from a file with `-/force_key_frames:0` lifts the argument limit, but brings back a per-attempt file on disk that the worker must create and remove on every exit path, which this decision otherwise deletes along with the output directory; standard input carries the stop command, and `ProcessBuilder` passes FFmpeg no other descriptor.
* Serving a replacement attempt's own init mid-playlist, or switching H.264 to the `avc3` sample entry so that parameter sets travel in-band, would tolerate a backend change, but the first needs per-attempt inits and playlist changes that ADR 0008 does not generate, and the second has no evidence on AVPlayer.
* Zeroing each attempt's timeline at its own first fragment gives a replacement attempt a different origin from the initial attempt, so the same segment number would name different media.
* Having the worker probe the source's start time and subtract it duplicates what `-start_at_zero` already does inside FFmpeg.
* Exempting the upload wait from every bound, on the grounds that back-pressure is not a stall, leaves an attempt that a silent receiver can hold forever.
* Counting a stopping attempt against the slots until the worker sends `JobAttemptStopped` would make the worker refuse a job that the server dispatched into the slot it freed when it sent `StopVariant`, although the stopping producer admits no new memory.
* Giving each producer only its own budget lets a job in a freed slot start at once, but it leaves the worker's segment memory unbounded by the slot count: each stopping producer holds up to two segment caps until FFmpeg exits, which for a hung FFmpeg takes the grace period and a forced kill, and a burst of seeks can stop attempts faster than their FFmpeg processes exit.
* Releasing a stopping producer's segments only when its stop settles after FFmpeg exits keeps the worker-wide bound, but it holds a new attempt's FFmpeg behind another attempt's exit, up to the grace period and a forced kill, for segments that the stop has already abandoned.
* Keeping MPEG-TS behind a worker capability flag changes nothing for the server, which would stop requesting MPEG-TS anyway, and both repositories would have to keep two container paths correct.
* Keeping a one-value `ContainerFormat` type as a slot for a future container preserves a decision that no code makes; a future container would need its own ADR, and its type can return then.
* Parsing FFmpeg's playlist or its `EXT-X-ENDLIST` tag as the completion signal appears in none of the surveyed projects, depends on a playlist the server already generates itself, and still reads a filesystem side channel.
* Scraping FFmpeg's log for segment events turns a log line into a contract, and a log line is not one.

== References

* Worker issue #36, worker PR #38, worker issue #1 (transferred from streamarr-server issue #253).
* Worker issue #40 (the producer's waits, memory and slots) and streamarr-server issue #433 (fMP4-only HLS delivery), whose pull request records the real-player check.
* streamarr-server issue #399 (playback capabilities epic) and its sub-issues #66, #396, #398 and #401; worker issues #14, #16, #18, #23, #25 and #28; ADR 0036 (streamarr-adr PR #16), whose vocabulary this decision follows.
* Worker issue #39 and, in the worker repository, its recorded fixtures (`src/test/resources/fmp4`), whose README states what each recording pins and lists each disagreement with the HLS muxer and its cause, and the Node recorder that made them (`src/test/fmp4-recorder`), which CI runs without FFmpeg to re-derive every recording's expectations.
* Worker issue #44, which records a variable-frame-rate source whose gap between frames outlasts the period and proposes a seek that keeps the frame the gap repeats.
* streamarr-server issue #446, which has the server refuse a stream session that would encode video without a usable probed frame rate.
* The write-ups of the proofs of concept behind this decision, with their harnesses, measurements and prior-art surveys, were not committed; the Context summarises their conclusions.
* The GOP, suppressed-keyframe, audio-rate and forced-keyframe-mode recordings of 2026-09-23 used the same pinned worker image and were not committed either, so the Context gives their results; the GOP and suppressed-keyframe recordings forced keyframes with the expression in place of the list, and the recorded fixtures repeat floored-GOP and suppressed-keyframe cases under the list, at 23.976 fps from the start.
* https://ffmpeg.org/ffmpeg.html#Advanced-options[FFmpeg advanced options: `-copyts`, `-start_at_zero`, `-fps_mode`] and https://github.com/FFmpeg/FFmpeg/blob/n8.0/fftools/ffmpeg_mux_init.c[`ffmpeg_mux_init.c`: automatic frame synchronization under `-copyts`]
* https://ffmpeg.org/ffmpeg-formats.html#mov_002c-mp4_002c-ismv[FFmpeg mov/mp4 muxer options: `movflags`, `frag_keyframe`, `min_frag_duration`, `frag_discont`]
* https://github.com/FFmpeg/FFmpeg/blob/n8.1/libavformat/hlsenc.c#L2450-L2489[FFmpeg n8.1 `hlsenc.c`: the cut rule measures from the run's first packet, whose `pts` it keeps in that packet's own time base] and https://github.com/FFmpeg/FFmpeg/blob/n8.0/libavformat/movenc.c[`movenc.c`: the mp4 muxer declares `AVFMT_VARIABLE_FPS`]
* https://github.com/FFmpeg/FFmpeg/blob/n8.1/fftools/ffmpeg_enc.c#L734-L781[FFmpeg n8.1 `ffmpeg_enc.c`, `forced_kf_apply`: a list compares each frame's own timestamp with absolute times and consumes one entry per frame, while an expression's `t` counts from the first frame] and https://github.com/FFmpeg/FFmpeg/blob/n8.1/fftools/ffmpeg_mux_init.c#L3175-L3299[`ffmpeg_mux_init.c`: parsing the list and choosing the mode]
* Worker issue #43, which records that SVT-AV1's `av1C` sequence header differs from the one in its stream, and proposes rewriting it from the stream.
* Worker issue #42, which records the SVT-AV1 study, and https://gitlab.com/AOMediaCodec/SVT-AV1/-/work_items/2385[SVT-AV1 issue #2385: the packetization reorder queue overruns and the output buffers run out], fixed by commit `ae2658e5` (merge request !2765) on SVT-AV1's master branch.
* https://github.com/FFmpeg/FFmpeg/blob/n8.1/fftools/ffmpeg.c#L147-L160[FFmpeg n8.1 `ffmpeg.c`: the signal handler counts signals and exits only on the fourth], https://github.com/FFmpeg/FFmpeg/blob/n8.1/fftools/ffmpeg.c#L906-L921[its main loop ends on a signal or `q` and then stops the scheduler], https://github.com/FFmpeg/FFmpeg/blob/n8.1/fftools/ffmpeg_sched.c#L2699-L2716[`ffmpeg_sched.c`: stopping joins every thread] and https://github.com/FFmpeg/FFmpeg/blob/n8.1/fftools/ffmpeg_mux.c#L177-L199[`ffmpeg_mux.c`: a non-monotonic decode time becomes the previous one plus one]
* https://developer.apple.com/documentation/http-live-streaming/hls-authoring-specification-for-apple-devices/[Apple HLS authoring specification: container requirements per codec]
* https://github.com/moq-dev/moq/blob/7ee2b0241ef3c2e942808c84f2dd39d8253e0f72/rs/moq-mux/src/container/fmp4/import.rs[moq-mux fMP4 importer] and https://github.com/moq-dev/moq/blob/7ee2b0241ef3c2e942808c84f2dd39d8253e0f72/rs/moq-mux/src/container/consumer.rs[its container consumer, which skips stale groups]
* https://datatracker.ietf.org/doc/html/rfc8216[RFC 8216: HTTP Live Streaming]
